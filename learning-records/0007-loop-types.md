# for...of vs for...in

Student correctly reasoned that `for...of` iterates values and `for...in` iterates keys/indexes. Predicted outputs correctly before running code.

## Evidence
- Correctly distinguished the two constructs
- Predicted behavior before execution

## Implications
- Student can now choose the right loop type based on what they need
- Ready for exercises that require iterating over object entries (Object.entries)
