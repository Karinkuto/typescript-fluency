# Composite Matching

Student solved aggregation by exporter + grade, correctly identifying that duplicate condition requires both fields to match (AND logic), not just one.

## Evidence
- Initially unclear whether matching was by exporter alone
- Independently reasoned that `same exporter AND same grade` defines a duplicate
- Student's own realization, not told the answer

## Implications
- Strong signal that boolean logic is improving
- Ready for exercises involving multiple conditions (filtering, validation)
- Can introduce compound conditions in filter/find with confidence
